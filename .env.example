# ── Environment ──
ENV=development

# ── Database ──
DATABASE_URL=postgresql+asyncpg://honeywell:changeme@db:5432/honeywell_sales
POSTGRES_DB=honeywell_sales
POSTGRES_USER=honeywell
POSTGRES_PASSWORD=changeme

# ── Auth ──
JWT_SECRET_KEY=   # REQUIRED - generate: python3 -c "import secrets;print(secrets.token_hex(32))"
ACCESS_TOKEN_EXPIRE_MINUTES=30
DEFAULT_ADMIN_EMAIL=admin@honeywell.com
DEFAULT_ADMIN_PASSWORD=ChangeMe123!

# ── CORS ──
CORS_ORIGINS=http://localhost,http://localhost:80

# ── Anthropic (Claude API - optional) ──
ANTHROPIC_API_KEY=

# ── Microsoft Graph API (optional) ──
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
GRAPH_USER_EMAIL=

# ── Notification Channels (Slack/Teams - optional) ──
SLACK_WEBHOOK_URL=
TEAMS_WEBHOOK_URL=

# ── Internal email domains ──
INTERNAL_EMAIL_DOMAINS=honeywell.com

# ── SMTP (optional) ──
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=

# ── Company ──
COMPANY_NAME=Honeywell Turkey

# ── Quote Defaults ──
QUOTE_PREFIX=HW-2026-
DEFAULT_TAX_RATE=20
DEFAULT_CURRENCY=TRY

# ── Rate Limiting ──
RATE_LIMIT_LOGIN=5/minute
RATE_LIMIT_API=100/minute

# ── Security ──
MAX_UPLOAD_SIZE_MB=10
