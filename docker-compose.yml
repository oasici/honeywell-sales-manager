services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-honeywell_sales}
      POSTGRES_USER: ${POSTGRES_USER:-honeywell}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-honeywell}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Redis service removed — see app/core/redis_client.py for the
  # rationale. Caches degrade to in-memory; multi-worker deploys
  # lose cross-worker consistency. Re-add the ``redis:`` service +
  # depends_on entries below if a Redis-equivalent is reintroduced.

  backend:
    build: ./backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-honeywell}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-honeywell_sales}
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-4}
      SCHEDULER_ENABLED: "false"
    volumes:
      - backend_data:/app/data
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  scheduler:
    build: ./backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-honeywell}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-honeywell_sales}
      SCHEDULER_ENABLED: "true"
    command: ["python", "-m", "app.tasks.run_scheduler"]
    volumes:
      - backend_data:/app/data

  qdrant:
    image: qdrant/qdrant:v1.13.2
    restart: unless-stopped
    volumes:
      - qdrant_data:/qdrant/storage
    ports:
      - "6333:6333"
    healthcheck:
      # qdrant image doesn't ship with curl/wget. Use bash's /dev/tcp to probe HTTP 200.
      test:
        [
          "CMD-SHELL",
          "bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333 && echo -e \"GET /healthz HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3 && head -n 1 <&3 | grep -q \"200\"'",
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:8080"

volumes:
  pgdata:
  backend_data:
  qdrant_data:
