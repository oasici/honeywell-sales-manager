apiVersion: v1
kind: Service
metadata:
  name: hsm-backend
  namespace: hsm
  labels:
    app: hsm-backend
spec:
  type: ClusterIP
  selector:
    app: hsm-backend
  ports:
    - name: http
      port: 80
      targetPort: http

